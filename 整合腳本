local Scripts = {
    ["快速攻擊"] = function()
        print("快速攻擊已啟用")
        -- 在這裡放快速攻擊的程式碼
    end,
    ["ESP顯示"] = function()
        print("ESP已啟用")
        -- 
    end,
    ["自動跳躍"] = function()
        print("自動跳躍已啟用")
        -- 在這裡放自動跳躍程式碼
    end
}
